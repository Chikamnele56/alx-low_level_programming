#include "main.h"

/**
 * _abs -> return absolute value of a number
 * @n: parameter
 * Return: absolute value of a number
 */
int _abs(int n)
{
	{
		if (n < 0)
			return (n);
	}
	else
	{
		if (n > -n)
			return (-n);
	}
	_putchar('\n');
	return (0);
}
